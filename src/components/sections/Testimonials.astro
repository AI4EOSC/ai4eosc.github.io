---
import data from '../../data/testimonials.yml';
---

<section>
  <div class="section-label">
    <div class="container">{data.label}</div>
  </div>
  <div class="container">
    <div class="grid">
      {data.items.map((item) => (
        <article class="card glass" key={item.name}>
          <p class="quote">{item.quote}</p>
          <div class="author">
            <div class={`avatar ${item.tone}`}>
              {item.initials}
            </div>
            <div>
              <div class="name">{item.name}</div>
              <div class="role">{item.role}</div>
            </div>
          </div>
        </article>
      ))}
    </div>
  </div>
</section>

<style>
  .grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 12px;
    padding-bottom: 20px;
  }

  .card {
    border-radius: var(--radius-md);
    padding: 22px;
  }

  .quote {
    font-size: 15px;
    color: var(--color-neutral-600);
    line-height: 1.5;
    margin: 0 0 18px;
    font-style: italic;
  }

  .quote::before {
    content: '';
    display: block;
    width: 24px;
    height: 2px;
    margin-bottom: 12px;
    background: linear-gradient(90deg, var(--color-primary-400), transparent);
    border-radius: 1px;
  }

  .author {
    display: flex;
    align-items: center;
    gap: 15px;
  }

  .avatar {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 11px;
    font-weight: 600;
    flex-shrink: 0;
  }

  .avatar.secondary {
    background: var(--color-secondary-100);
    color: var(--color-secondary-700);
    box-shadow: 0 0 0 3px rgba(236, 93, 128, 0.15), 0 0 12px rgba(236, 93, 128, 0.20);
  }

  .avatar.primary {
    background: var(--color-primary-100);
    color: var(--color-primary-700);
    box-shadow: 0 0 0 3px rgba(0, 135, 146, 0.15), 0 0 12px rgba(0, 135, 146, 0.20);
  }

  .name {
    font-size: 16px;
    font-weight: 600;
    line-height: 1.1;
    color: var(--color-neutral-900);
  }

  .role {
    font-size: 14px;
    font-weight: 500;
        line-height: 1.1;
    color: var(--color-neutral-400);
  }

  @media (max-width: 900px) {
    .grid {
      grid-template-columns: 1fr;
    }
  }
</style>
